# 📦 安装与启动指南

## ✅ 当前状态

前端项目已完成配置，包含：

### 核心依赖
- ✅ Next.js 14 (App Router)
- ✅ TypeScript
- ✅ Tailwind CSS
- ✅ Framer Motion (动画)
- ✅ Lucide React (图标)
- ✅ Markdown-it (Markdown 渲染)
- ✅ React Markdown (备用 Markdown 渲染)
- ✅ Recharts (图表)
- ✅ clsx + tailwind-merge (className 工具)

### 配色方案 (已更新)
```css
背景色:   #121212  (深黑色，硬核户外感)
次级背景: #1E1E1E
文字色:   #F5F5F5
强调色:   #2D5A27  (森林绿)
```

## 🚀 快速启动

### 第 1 步：安装依赖

```bash
cd frontend
npm install
```

**预期耗时**: 1-2 分钟

### 第 2 步：启动开发服务器

```bash
npm run dev
```

**预期输出**:
```
▲ Next.js 14.0.4
- Local:        http://localhost:3000
- Ready in 2.5s
```

### 第 3 步：访问应用

浏览器打开: **http://localhost:3000**

---

## 🧪 完整测试流程

### 1️⃣ 启动后端（终端 1）
```bash
cd backend
source venv/bin/activate
python main.py
```

验证: http://localhost:8000/api/docs

### 2️⃣ 启动前端（终端 2）
```bash
cd frontend
npm install  # 首次运行
npm run dev
```

访问: http://localhost:3000

### 3️⃣ 测试数据生成

填写表单：
```
出发地: Portland, OR
目的地: Crater Lake, OR
出发日期: 2024-02-15
行程天数: 7
车型: Crossover
车型型号: Mazda CX-50
兴趣: 徒步, 攀岩, 科学解释
活动强度: 挑战
✓ 包含非铺装路段
✓ 包含详细科学解释
```

### 4️⃣ 观察加载动画

点击"生成专家级路书"后，观察动态文案：
```
1. 地层扫描中...
2. 计算 CX-50 离地间隙适配度...
3. 正在分析地质构造...
4. 评估非铺装路面通过性...
5. 计算补给点与加油站分布...
6. 生成攀岩难度评级...
7. 整合物流保障数据...
8. 渲染专家级路书...
```

### 5️⃣ 查看结果页

- **左侧**: Markdown 格式路书（长滚动）
- **右侧**: 固定位置数据卡片
  - 💰 预算饼图
  - 🔬 地质科普
  - ⚠️ 物流安全提示

---

## 📂 项目结构

```
frontend/
├── app/
│   ├── layout.tsx          # 全局布局
│   ├── globals.css         # 全局样式 (更新背景色)
│   ├── page.tsx            # 首页 (表单 + 加载动画)
│   └── result/
│       └── page.tsx        # 结果页
│
├── lib/
│   └── utils.ts            # clsx + tailwind-merge 工具
│
├── package.json            # 依赖 (已更新)
├── tailwind.config.ts      # Tailwind (背景 #121212)
└── tsconfig.json           # TypeScript 配置
```

---

## 🎨 样式特色

### 背景色更新
```css
/* 从 #1A1A1A 更新为 #121212 */
background-color: #121212;  /* 更深、更硬核 */
```

### 强调色
```css
forest-green: #2D5A27;      /* 森林绿，户外感 */
```

### Glass Effect
```css
.glass-effect {
  background: rgba(30, 30, 30, 0.5);  /* 更新为新的 carbon-light */
  backdrop-blur: 12px;
}
```

---

## 🔧 核心功能

### 1. 极简表单
- 目的地输入
- 车型选择（支持 Mazda CX-50 等）
- 活动偏好（徒步/攀岩/科学解释）
- 非铺装路段选项

### 2. 动态加载动画
- 旋转 Mountain 图标（360° 无限循环）
- 8 条动态文案，每 2 秒切换
- 进度条渐变效果

### 3. 结果页布局
- **左侧 (2/3)**: Markdown 路书
  - 7 天详细行程
  - 科学重点解读
  - 预算明细表
  
- **右侧 (1/3)**: 固定卡片
  - 预算饼图（Recharts）
  - 地质科普点
  - 物流安全提示

### 4. 后端连接
```typescript
POST http://localhost:8000/api/itinerary/generate

// 响应后存储到 sessionStorage
sessionStorage.setItem('itinerary', JSON.stringify(data))

// 自动跳转
router.push('/result')
```

---

## 🐛 常见问题

### Q1: npm install 失败
```bash
# 清除缓存
rm -rf node_modules package-lock.json
npm cache clean --force
npm install
```

### Q2: 端口 3000 被占用
```bash
# 使用其他端口
npm run dev -- -p 3001

# 或杀死占用进程
lsof -ti:3000 | xargs kill -9
```

### Q3: TypeScript 报错
```bash
# 重新安装依赖
npm install
```

### Q4: 后端连接失败
**检查**:
1. 后端是否运行? (http://localhost:8000)
2. CORS 配置是否正确?
3. 浏览器控制台有无错误?

---

## 📝 开发命令

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 启动生产服务器
npm start

# 代码检查
npm run lint
```

---

## 🎯 下一步

1. ✅ 测试完整流程（表单 → 加载 → 结果）
2. ✅ 验证后端连接
3. ✅ 检查数据展示
4. 🔄 根据反馈调整样式

---

**✨ 前端已就绪，随时可以启动测试！**
